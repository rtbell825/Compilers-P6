int func(int a, bool b) {
	return a;
}

void main () {
     int x;
     int y;
     bool b;
     y = func(x, b);
     if (y == 0) {
     	int x;
     	int y;
     }

     x = 5;
     b = true;
     b = false;
     return;
}
